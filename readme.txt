I created a file rotation system for garageFS because they don't currently support versioning / file rotation

Logic:

Services running on SystemD timers automate a file check process as well as the lifecycle process.
